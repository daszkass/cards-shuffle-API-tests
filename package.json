{
  "name": "cards-shuffle-api-tests",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "k6": "k6 run k6/draw-cards.js",
    "pw": "npx playwright test",
    "pw:coffee": "npx playwright test --project=coffee",
    "pw:cards": "npx playwright test --project=deckOfCards",
    "pw:api": "npx playwright test --grep @api",
    "pw:ui": "npx playwright test --grep @ui"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@playwright/test": "^1.53.1",
    "@types/node": "^24.0.3"
  },
  "dependencies": {
    "dotenv": "^16.5.0"
  }
}
